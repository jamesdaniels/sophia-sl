<h1>Slides</h1>    
<% paginated_section @posts do %>
	<ul class="slides">
	<% @slides.each do |slide| %>
	  <%= content_tag :li, link_to(image_tag(formatted_slide_url(slide, :jpg), :height => 60), slide), :class => 'slide' %>
	<% end %>    
	</ul>
<% end %>
